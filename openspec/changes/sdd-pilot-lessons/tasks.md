## 1. Skill and repository files

- [ ] 1.1 `meta-github-workflow`: delete `references/spec-expression.md` and `assets/workflow-spec-archive.yml`; remove the `{{SPEC_*}}` placeholders from `assets/project-skill.md`, the `spec` field from the task and feature forms, the specification lines from `assets/pull-request-template.md` and `assets/tracking-issue.md`, and the specification passages from `SKILL.md`, `decision-tree.md`, `durable-harness.md`, `issues-and-prs.md`, `semantic-mapping.md`, `rules-and-protection.md`, `ci-and-runners.md`, `security-and-governance.md`; add `## Extension slots` to `references/durable-harness.md`, the step-5 slot check and the hand-back sentence to `SKILL.md`, the check-ordering rule to `rules-and-protection.md`; verify `just check-skill skills/meta/meta-github-workflow` and the residue grep — closes GH: Specification contract present at build time, No paradigm contract, New check named
- [ ] 1.2 `meta-gitlab-workflow`: the symmetric edits (delete `references/spec-expression.md` and `assets/gitlab-ci-spec-archive.yml`; strip `work-items-and-mrs.md`, the templates, `project-skill.md`, and the references; add `## Extension slots`, the step-5 check, the hand-back, and the thread-resolution baseline to `security-and-governance.md`); verify `just check-skill skills/meta/meta-gitlab-workflow` and the residue grep — closes GL: Specification contract present at build time, No paradigm contract, Baseline proposed
- [ ] 1.3 `meta-spec-workflow/scripts/archive_completed_changes.py`: move byte-identical from the engineering skill, then add the spec-less sentence to the docstring and epilog; verify `python3 … --help` — closes MSW Script: Help
- [ ] 1.4 `meta-spec-workflow/references/tracked-work-lifecycle.md` (design basis moved from SDD: shape table, timing table, archive job rules, per-tool notes) and `references/github-expression.md`, `references/gitlab-expression.md` (slot-keyed edit tables, fill contract, push path by owner type, validator-into-check-command step, KNOWLEDGE_SECTION text); verify `just check-skill skills/meta/meta-spec-workflow` — closes Base delivered on GitHub, Slot already filled, User-owned repository, Protected branch refuses the token, Validator joins the check command
- [ ] 1.5 `meta-spec-workflow/assets/github/{template-lines.md,project-skill-steps.md,workflow-spec-archive.yml}` and `assets/gitlab/{template-lines.md,project-skill-steps.md,ci-spec-archive.yml}`; verify `just check-skill` — closes Combined shape, no specification yet; Split shape; Checklist check still passes; Draft carries no implementation; All threads resolved; Unresolved thread; Requested adjustment missing from the record; OpenSpec with automation; Spec-Kit with automation
- [ ] 1.6 `meta-spec-workflow/assets/spec-workflow.md` (`## Artifact operations`, approval modes with reconciliation, freeze rule and push-path slots, body lines and reserved sections, scope) and `references/durable-output.md` (must-carry list, the reversed template-line ownership); verify `just check-skill` — closes GitHub project deposit, Reading the specification scope, Tool without a validator, Approval mode left unspecified, Tooling beside the product
- [ ] 1.7 `meta-spec-workflow/references/{openspec,spec-kit,kiro,committed-documents}.md`: `## Commands and validation` paragraphs, the spec-less marker and archive timings in the OpenSpec one; verify `just check-skill` — closes Scaffolding by command, Spec-Kit with automated archiving, OpenSpec harness change, Propagation recorded as Dependency, No automation can push
- [ ] 1.8 `meta-spec-workflow/SKILL.md`: step 1 (base and contract present?), step 2 questions 3 and 8, step 6 hand-off order, new step 7 `Shape the platform`, step 8 close, `compatibility`, gotchas, and the description under 900 characters; verify `just check-skill` and the description length — closes MSW Trigger: Harness alignment request, Delivered base awaits shaping, Writing a specification; No base yet
- [ ] 1.9 `spec-driven-development/SKILL.md`: cut to the methodology (levels, when it pays, approach families, the loop under the contract with publish-then-stop, reconcile, verify, converge or archive), `## Tool commands first, then the validator`, `## Project rules live in the contract`, `## Setting up or improving the project's rules`, gotchas, `compatibility`; delete `scripts/archive_completed_changes.py`; verify `just check-skill skills/engineering/spec-driven-development` — closes every SDD MODIFIED and ADDED scenario listed against SKILL.md in the placement table and SDD REMOVED Script
- [ ] 1.10 `spec-driven-development/references/tracked-work-lifecycle.md` rewritten as the run-time reference (approval modes, reconciliation checklist, default request body, archive modes at run time, per-tool loop notes with command categories and validation points) and `references/adopting-existing-code.md` (product boundary, baseline defect, builder by role); verify `just check-skill` — closes Draft body in the specification phase, Implementation offered for the draft, Failing baseline scenario, Tool without a validator, Hand-written record offered
- [ ] 1.11 `meta-agent-authority`: `SKILL.md` gate paragraphs, `references/authority-profiles.md`, `assets/agent-authority.md` Gates table and report section, `references/durable-output.md` report items; verify `just check-skill skills/meta/meta-agent-authority` — closes AA: H1 offered with an agent-authored specification, Agent asked to approve and ready, Discussion-closed gate under H1, Reading the deposited report format
- [ ] 1.12 `meta-workflow-design/SKILL.md` hand-off order and `assets/platform-workflow.md` `## Other contracts`; verify `just check-skill skills/meta/meta-workflow-design` — closes WD: Hand-off order names the second phase, Deposited file without a paradigm contract

## 2. External impact

- [ ] 2.1 `skills/meta/CONTEXT.md` (paradigm builders, contract-flow rule, dependency list) and `skills/engineering/CONTEXT.md` disambiguation row; verify `just validate`
- [ ] 2.2 README pair rows: `skills/meta/README.md` + `README.zh.md` (meta-spec-workflow, both bases) and `skills/engineering/README.md` + `README.zh.md` (SDD); verify `just validate`
- [ ] 2.3 `.claude-plugin/marketplace.json` `meta` plugin description by hand; verify `just gen-marketplace` leaves it unchanged
- [ ] 2.4 `meta-harness-building/assets/harness-plan-template.md` Build List phased rows; verify `just check-skill skills/meta/meta-harness-building`
- [ ] 2.5 Hand-edit the `## Purpose` lines of `openspec/specs/meta/meta-github-workflow/spec.md` and `openspec/specs/meta/meta-gitlab-workflow/spec.md`; verify `just spec-validate`
- [ ] 2.6 Companion change `sdd-pilot-lessons-harness` implemented (mirror origin, register rows, this repository's gate, reviewer rule, `ARCHITECTURE.md`); verify `just validate` and `diff scripts/archive_completed_changes.py skills/meta/meta-spec-workflow/scripts/archive_completed_changes.py` empty

## 3. Tests

- [ ] 3.1 Trigger cases t1, t2, t3 in the fixture project — closes MSW Trigger: Delivered base awaits shaping, Harness alignment request, Writing a specification
- [ ] 3.2 Outcome task o1 — closes SDD: Draft body in the specification phase, Implementation offered for the draft
- [ ] 3.3 Outcome task o2 — closes SDD: Waiting after publication, No contract, Handoff offered
- [ ] 3.4 Outcome task o3 — closes SDD: Unresolved thread, All threads resolved, Requested adjustment missing from the record
- [ ] 3.5 Outcome task o4 — closes SDD: CI workflow change, Domain requested for tooling
- [ ] 3.6 Outcome task o5 with the OpenSpec CLI in the fixture — closes SDD: Tool scaffolds the change, Validator available, Hand-written record offered
- [ ] 3.7 Readback r3 (SDD questionnaire) — closes the fifteen SDD scenarios listed in the plan's r3 row
- [ ] 3.8 Readback r2 (meta-spec-workflow questionnaire) — closes the twenty-three MSW scenarios listed in the plan's r2 row
- [ ] 3.9 Readback r1 (both bases) — closes GH/GL: Specification contract present at build time, No paradigm contract, New check named, Baseline proposed
- [ ] 3.10 Template harness with `scripts/check_pr_policy.py` — closes GH/GL: Security line survives a slot fill; MSW: Checklist check still passes, Draft carries no implementation
- [ ] 3.11 Readback r5 (meta-agent-authority) — closes the four AA scenarios
- [ ] 3.12 Readback r4 (hand-off order across meta-workflow-design, meta-spec-workflow, CONTEXT.md, harness-plan template) — closes the two WD scenarios
- [ ] 3.13 Script harness in a scratch worktree (Help, Nothing completed, Representative run, Spec-less change, Repeated run, Bad arguments) and the mirror diff — closes MSW Script: Help, Representative run, Spec-less change, Nothing completed, Repeated run, Bad arguments
- [ ] 3.14 Record skipped cases and their reasons for the pull request's Validation section

## 4. Finish

- [ ] 4.1 Run `just check`, write the results to the pull request's Validation section linking the verification plan, fill Changes with permalinks, set `Phase: implementation`, archive both changes inside the pull request, and mark it ready
