## 1. Skill and repository files

- [x] 1.1 `skills/scaffold/scaffold-ml/SKILL.md`: rewrite as the single shape — description (marker kept, under 900 characters), Inspect first, The shape, Workflow steps 1–13 (inventory; framework; carrier question; configuration surface; manifest and tracker; tests, hooks, style with reasons; research-task convention; containers opt-in with the GPU container handoff; deposited guidance; verification; workflow and authority handoff; durable skills by role; harness entry and closing), Deposited decisions, Done when, Gotchas; verify `just check-skill skills/scaffold/scaffold-ml` — closes Empty training repository, Early repository with one script, Inference service (near-miss), Data pipeline (near-miss), Empty repository, Existing Hydra project, Evaluation before the first experiment, Transformation targets the raw cache, No preference stated, Existing requirements project, Fresh checkout, No accelerator on the build machine, Python defaults builder runs afterwards, Specification workflow builder runs afterwards, Entrypoint read after disposal, Marker leak, Handoff offered and User declines (four handoffs)
- [x] 1.2 References: rewrite `references/containers.md` (three stages, preinstalled-stack rule, digest and `IMAGE_DIGEST`, host facts, volumes, shared memory); add `references/config-surface.md`, `references/provenance-and-tracker.md`, `references/research-task.md`, `references/growing-the-layout.md`; move `references/training/uv-hardware-deps.md` to `references/hardware-deps.md` and `references/experiment/pinned-deps.md` to `references/requirements-lock.md` (four files, per-machine backends); delete `references/training/hydra-config.md` and `references/experiment/shared-module.md`; verify `just check-skill` and that no `](../` or `](/` link exists — closes Optimizer selection, Resolved dump, Requirements carrier chosen, uv project chosen, Development machine differs from the training box, New project on GitHub, Existing tracker, Dirty tree at launch, Container requested, No container requested, Preinstalled-stack image chosen, OpenSpec project, No spec tool
- [x] 1.3 Assets: add `assets/train.py`, `assets/eval.py`, `assets/config.py`, `assets/configs/config.yaml`, `assets/stages.py`, `assets/run_manifest.py` (copy of the durable skill's), `assets/requirements.in`, `assets/requirements.dev.in`, `assets/pyproject-tool-config.toml`, `assets/pre-commit-config.yaml`, `assets/justfile`, `assets/Dockerfile`, `assets/compose.yaml`, `assets/devcontainer.json`, `assets/agents-md.md`, `assets/docs-data.md`, `assets/research-spec.md`, `assets/openspec/research-task/schema.yaml` and `templates/{research,hypotheses,tasks}.md`; delete `assets/experiment/` and `assets/training/`; verify `ruff check`/`ruff format --check` on the Python assets and `python3 -m py_compile` — closes GPU test without a GPU, Hook runs tests, Marker leak
- [x] 1.4 Apply the pull-request review findings: `assets/dockerignore`; the environment at `/opt/venv` with the revision label and `GIT_COMMIT` build argument; one broadcast run id; the dirty-tree refusal with `run.allow_dirty`; the manifest before the tracker with its scalars as parameters; optimizer-step counting and a bound `grad_norm`; mixed-precision normalization before the resolved dump; the try/finally from the manifest on; the eval child manifest; the just feature and GPU-flag placeholder in the dev container; `uv sync --locked`; the shared-memory sentence; the status-versus-degraded sentence; the framework evaluation on JAX signals with `references/framework-choice.md` and the JAX section of `references/hardware-deps.md`; the empty `IMAGE_DIGEST` normalization and the stamped `GIT_COMMIT` fallback in both manifest copies — closes Run inside a sealed image, Ordinary training project, JAX signal present, Existing JAX project, Dirty tree at launch, Throwaway run from a dirty tree, Ignored artifacts, Multi-device launch, Evaluation of a run, Source mounted over the image, Sealed image built
- [x] 1.5 Template ceiling: trim `assets/train.py` to the loop's seams with business logic as placeholders (model, optimizer, loader, scheduler, forward pass, metrics; `model.train()` after the cadence evaluation; the gradient norm logged only when the clipping seam returned one; `load_config` annotated `DictConfig`), `assets/config.py` to the control-plane fields with project values `MISSING`, `assets/configs/config.yaml` to the values the schema leaves unset; rewrite the step 4 and step 5 pointers (copy and fill); verify `ruff check`, `ruff format --check`, `py_compile` — closes Optimizer selection, Resolved dump, Evaluation of a run
- [x] 1.6 One pointer per asset, comments to the pointers: `justfile` keeps the uv-project recipes (requirements recipes → fenced block in `references/requirements-lock.md`; docker recipes → `## Task-runner recipes` in `references/containers.md`, whose `## Assets` section moves into step 8); builder-addressed lines leave `pyproject-tool-config.toml`, `pre-commit-config.yaml`, `gitignore`, `dockerignore`, `Dockerfile` (uv image pinned to a release tag; requirements stage → fenced block in the reference), `compose.yaml`, `devcontainer.json` (`runArgs` holds `--shm-size` only); `agents-md.md` drops the hyperparameter vocabulary bullet; `docs-data.md` drops its rules; `assets/research-spec.md` deleted and `templates/research.md`, `hypotheses.md`, `tasks.md` reduced to skeletons with the shape rules in `schema.yaml`; steps 3, 6, 7, 8, 9 carry the pointers; `references/research-task.md` points the no-tool path at the templates; verify `just check-skill` and `grep` for builder-addressed phrases in assets — closes No spec tool, Container requested, Marker leak, Entrypoint read after disposal
- [x] 1.7 Gotchas and review findings: move the disposable-builder exclusion into the workflow, the PyTorch-only entry-point rule into `## The shape` with the JAX dependency and base-image sentence (also in `references/framework-choice.md` and `references/requirements-lock.md`), delete the bullets a step or reference states, add the `mixed_precision=no` and just-syntax facts; widen `assets/gitignore`; `eval.py` hashes with `sha256_file` and evaluates under the recorded precision; `stages.py` books time in `finally`; steps 11 and 13 carry their own installer instruction; both `run_manifest.py` copies export `sha256_file` and mark a declared-but-empty `IMAGE_DIGEST` as `no_image_digest`; verify `just check-skill` and `cmp` of the two copies — closes JAX signal present, Compose run without a digest, Ignored artifacts, Handoff offered (work tracking; harness entry)

## 2. External impact

- [x] 2.1 Rewrite the `scaffold-ml` row in `skills/scaffold/README.md` and `README.zh.md` (content-identical) and the `scaffold` plugin description in `.claude-plugin/marketplace.json` if its wording moves; run `just gen-marketplace`; verify `just validate` and that both README rows name the same capabilities
- [x] 2.2 Confirm `git diff --stat origin/main...HEAD -- skills/meta skills/machine-learning skills/core skills/engineering skills/writing scripts` shows only `skills/machine-learning/experiment-provenance/assets/run_manifest.py` (the shared mirror correction) and that both manifest copies are identical

## 3. Tests

- [x] 3.1 Run the two Trigger cases as clean-context Sonnet-class subagents in the fixture repository — closes Empty training repository, Data pipeline (near-miss)
- [x] 3.2 Run the readback case: one clean-context subagent quotes the passage behind every remaining scenario and checks the marker, links, handoff patterns, placeholders, and load sentences; fix any GAP and rerun — closes every scenario in the plan's readback list
- [x] 3.3 Run the asset harness: compile and lint the Python assets, diff the manifest copy, run the CPU smoke of `train.py` and `eval.py` in a scratch venv — a clean-tree run with gradient accumulation, the eval child run, the dirty-tree refusal and the `run.allow_dirty` override, and a two-process launcher run sharing one run id (or record why it could not run) — and validate the research-task schema — closes Resolved dump, Dirty tree at launch, Throwaway run from a dirty tree, Multi-device launch, Evaluation of a run, Fresh checkout
- [x] 3.4 Run the template-ceiling round of the asset harness (accumulation with a cadence evaluation and mode restore, resume, the checkpoint digest, a missing value, `IMAGE_DIGEST` declared empty / set / absent, a raising stage) — closes Optimizer selection, Resolved dump, Evaluation of a run, Compose run without a digest
- [x] 3.5 Record skipped solver-executed scenarios with the maintainer's budget reason and every isolation degradation for the pull request's Validation section; remove the fixture, the venv, and all outputs

## 4. Finish

- [ ] 4.1 Run `just check`, write the results to the pull request's Validation section linking this change's verification plan, fill Changes with permalinks, run `just spec-validate`, and keep the `Spec:` link at the change directory; archiving waits for the maintainer's instruction
