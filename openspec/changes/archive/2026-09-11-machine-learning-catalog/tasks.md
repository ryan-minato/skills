## 1. Skill and repository files

- [x] 1.1 `skills/machine-learning/experiment-provenance/`: `SKILL.md` (description; The run equation, Run identity, Source snapshot, Resolved run configuration, Inputs and lineage, Environment identity, The manifest, Where the facts live, Handoffs, Gotchas), `references/tracker-selection.md`, `references/run-record.md`, `assets/run_manifest.py`, `assets/run-record.md`; verify `just check-skill skills/machine-learning/experiment-provenance` — closes Reproducibility question, Tracker wiring, Research task organization (near-miss), Software build provenance (near-miss), Run record contents, Mutable references offered as identity, Performance claim without host facts, Config assembled from several sources, Correcting a past run's record, Naming a searched value, Launch from a dirty tree, Squash planned, Existing tracker, Prompts proposed as run metadata, Handoff offered, User declines
- [x] 1.2 `skills/machine-learning/research-workflow/`: `SKILL.md` (description; The research task, The research spec, The loop, Evidence must match the claim, Automatic search, Closing the task, Where each fact lives, Handoffs, Gotchas), `references/research-spec-fields.md`, `references/evidence-and-search.md`, `references/branching-and-pr.md`, `assets/research-spec.md`, `assets/hypothesis-log.md`; verify `just check-skill skills/machine-learning/research-workflow` — closes Organizing a research task, Closing with a negative result, Software feature spec (near-miss), Team process design (near-miss), Spec from a stated objective, Only a solution is offered, Search scope proves wrong, Several hypotheses under one objective, Request per hypothesis requested, Run launched after edits, Spec revised mid-task, Improvement claimed from one seed, Negative result, Cheap runs explicit space, Large-model scale, Handoff offered, User declines
- [x] 1.3 `skills/machine-learning/experiment-code-conventions/`: `SKILL.md` (description; Abstraction, Dependencies, The training loop, The configuration surface, Tests and hooks, Style, Performance and readability, Handoffs, Gotchas), `references/config-surface.md`, `references/vendoring-research-code.md`, `references/tensor-tests-and-docs.md`, `references/hot-path-performance.md`; verify `just check-skill skills/machine-learning/experiment-code-conventions` — closes Trainer or loop, Configuration doing too much, Web handler cleanup (near-miss), Generic refactoring (near-miss), Shared preprocessing, Similar loops different research goals, Repetition cited as the reason, New training project, Paper repository, Class paths in configuration, Existing composition framework, GPU test without a GPU, Hook that runs tests, Type checker proposed for model code, Unfusing for readability, Handoff offered, User declines
- [x] 1.4 `skills/machine-learning/training-instrumentation/`: `SKILL.md` (description; Principles, Correlation keys, The minimal set, Signal catalog, Severity and alerts, PyTorch skeleton, Privacy, Handoffs, Gotchas), `references/alerting-and-dashboards.md`, `references/model-health-metrics.md`, `references/model-family-signals.md`, `references/cluster-telemetry.md`, `references/framework-hooks.md`, `assets/torch_health.py`; verify `just check-skill skills/machine-learning/training-instrumentation` with no body-size warning — closes What to log, Alerts for training jobs, Incident already happening (near-miss), Service logging (near-miss), Loop without correlation keys, Full profiler always on, Small single-GPU project, Step as a metric label, Prompts in logs, Absolute gradient-norm alert requested, Utilization alert requested, Language-model pretraining, Policy optimization, Handoff offered, User declines
- [x] 1.5 `skills/machine-learning/training-diagnosis/`: `SKILL.md` (description; Method, Symptom index, Numerical instability and replay, Deep diagnosis, The evidence chain, Handoffs, Gotchas), the seven symptom references, `assets/evidence-chain.md`, `scripts/scan_series.py` (standard library, `--help`, exit 0/1/2 documented); verify `just check-skill skills/machine-learning/training-diagnosis` and `just lint` — closes Utilization drop, Numerical failure, Designing signals (near-miss), Import failure (near-miss), Collective timeout reported, Learning-rate change proposed as the fix, GPU idle with rising data wait, Memory grows every epoch, Checkpoint slow link idle, Reproduces everywhere, Reproduces on one device only, Diagnosis delivered, Evidence insufficient, Handoff offered, User declines, Help, Representative run, Repeated run, Bad arguments

## 2. External impact

- [x] 2.1 Symlinks `.agents/skills/<name> -> ../../skills/machine-learning/<name>` for the five skills, the `machine-learning` plugin entry in `.claude-plugin/marketplace.json` (hand-added with the first skill, then `just gen-marketplace` after each), and the README pair rows in `skills/machine-learning/README.md` and `README.zh.md`; verify `just validate` passes and the two READMEs are content-identical
- [x] 2.2 Confirm nothing outside `skills/machine-learning/` and the companion change's files moves: `git diff --stat origin/main...HEAD -- skills/core skills/engineering skills/meta skills/scaffold skills/writing scripts .agents` lists only the five symlinks

## 3. Tests

- [x] 3.1 Build the fixture project in the session scratch directory, copy the five candidate skills to the user skills directory, run the ten Trigger cases of the verification plan as clean-context Sonnet-class subagents, one per prompt, and record load decisions — closes Reproducibility question, Research task organization (near-miss), Organizing a research task, Software feature spec (near-miss), Trainer or loop, Web handler cleanup (near-miss), What to log, Incident already happening (near-miss), Numerical failure, Designing signals (near-miss)
- [x] 3.2 Run the readback cases: one clean-context subagent per skill quotes the passage behind every remaining Trigger, Behavior, and Handoff scenario and reports gaps; fix the skill on any gap and rerun — closes every scenario named in the plan's readback list
- [x] 3.3 Run the `scan_series.py` harness from an untracked directory: `--help`, representative run (plain and `--group-by rank`), repeated run with `diff`, `--bogus`, and a missing column — closes Help, Representative run, Repeated run, Bad arguments
- [x] 3.4 Record the skipped solver-executed scenarios with the maintainer's budget reason and every isolation degradation for the pull request's Validation section; remove the fixture, the copied skills, the harness, and all outputs

## 4. Finish

- [x] 4.1 Run `just check`, write the results to the pull request's Validation section linking this change's verification plan, fill Changes with permalinks, archive this change in-request (the Actions bypass is not granted), run `just spec-validate`, and point the `Spec:` link at the archive path
