## 1. Skill and repository files

- [x] 1.1 `skills/scaffold/scaffold-ml/SKILL.md`: rewrite as the single shape — description (marker kept, under 900 characters), Inspect first, The shape, Workflow steps 1–13 (inventory; framework; carrier question; configuration surface; manifest and tracker; tests, hooks, style with reasons; research-task convention; containers opt-in with the GPU container handoff; deposited guidance; verification; workflow and authority handoff; durable skills by role; harness entry and closing), Deposited decisions, Done when, Gotchas; verify `just check-skill skills/scaffold/scaffold-ml` — closes Empty training repository, Early repository with one script, Inference service (near-miss), Data pipeline (near-miss), Empty repository, Existing Hydra project, Evaluation before the first experiment, Transformation targets the raw cache, No preference stated, Existing requirements project, Fresh checkout, No accelerator on the build machine, Python defaults builder runs afterwards, Specification workflow builder runs afterwards, Entrypoint read after disposal, Marker leak, Handoff offered and User declines (four handoffs)
- [x] 1.2 References: rewrite `references/containers.md` (three stages, preinstalled-stack rule, digest and `IMAGE_DIGEST`, host facts, volumes, shared memory); add `references/config-surface.md`, `references/provenance-and-tracker.md`, `references/research-task.md`, `references/growing-the-layout.md`; move `references/training/uv-hardware-deps.md` to `references/hardware-deps.md` and `references/experiment/pinned-deps.md` to `references/requirements-lock.md` (four files, per-machine backends); delete `references/training/hydra-config.md` and `references/experiment/shared-module.md`; verify `just check-skill` and that no `](../` or `](/` link exists — closes Optimizer selection, Resolved dump, Requirements carrier chosen, uv project chosen, Development machine differs from the training box, New project on GitHub, Existing tracker, Dirty tree at launch, Container requested, No container requested, Preinstalled-stack image chosen, OpenSpec project, No spec tool
- [x] 1.3 Assets: add `assets/train.py`, `assets/eval.py`, `assets/config.py`, `assets/configs/config.yaml`, `assets/stages.py`, `assets/run_manifest.py` (copy of the durable skill's), `assets/requirements.in`, `assets/requirements.dev.in`, `assets/pyproject-tool-config.toml`, `assets/pre-commit-config.yaml`, `assets/justfile`, `assets/Dockerfile`, `assets/compose.yaml`, `assets/devcontainer.json`, `assets/agents-md.md`, `assets/docs-data.md`, `assets/research-spec.md`, `assets/openspec/research-task/schema.yaml` and `templates/{research,hypotheses,tasks}.md`; delete `assets/experiment/` and `assets/training/`; verify `ruff check`/`ruff format --check` on the Python assets and `python3 -m py_compile` — closes GPU test without a GPU, Hook runs tests, Marker leak

## 2. External impact

- [x] 2.1 Rewrite the `scaffold-ml` row in `skills/scaffold/README.md` and `README.zh.md` (content-identical) and the `scaffold` plugin description in `.claude-plugin/marketplace.json` if its wording moves; run `just gen-marketplace`; verify `just validate` and that both README rows name the same capabilities
- [x] 2.2 Confirm `git diff --stat origin/main...HEAD -- skills/meta skills/machine-learning skills/core skills/engineering skills/writing scripts` is empty

## 3. Tests

- [x] 3.1 Run the two Trigger cases as clean-context Sonnet-class subagents in the fixture repository — closes Empty training repository, Data pipeline (near-miss)
- [x] 3.2 Run the readback case: one clean-context subagent quotes the passage behind every remaining scenario and checks the marker, links, handoff patterns, placeholders, and load sentences; fix any GAP and rerun — closes every scenario in the plan's readback list
- [x] 3.3 Run the asset harness: compile and lint the Python assets, diff the manifest copy, run the CPU smoke of `train.py` and `eval.py` in a scratch venv (or record why it could not run), and validate the research-task schema — closes Resolved dump, Dirty tree at launch, Fresh checkout
- [x] 3.4 Record skipped solver-executed scenarios with the maintainer's budget reason and every isolation degradation for the pull request's Validation section; remove the fixture, the venv, and all outputs

## 4. Finish

- [x] 4.1 Run `just check`, write the results to the pull request's Validation section linking this change's verification plan, fill Changes with permalinks, archive this change in-request (the Actions bypass is not granted), run `just spec-validate`, and point the `Spec:` link at the archive path
