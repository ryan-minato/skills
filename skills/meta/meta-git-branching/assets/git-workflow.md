<!--
Raw shape for the branching contract deposited into the target project.
Rework every line against the project's real answers, delete every section the
selected model does not use, and remove every placeholder and this comment
before the file is written. A section left as a placeholder is worse than a
missing section, because it reads as settled.
-->

# Git Branching Contract

Read this before creating a branch, opening a pull or merge request, merging,
or creating a tag.

Model: <GitHub Flow | GitLab Flow, production branch | GitLab Flow, environment
branches | GitLab Flow, release branches | git flow>. Selected because
<one sentence naming the project fact that decided it>.

## Long-lived branches

| Branch | Meaning | Accepts merges from | Protection tier |
|---|---|---|---|
| `<default-branch>` | <what a commit here means> | <sources> | <enforced / advisory / convention> |

<!-- Delete this table's extra rows for a GitHub Flow project: the default
branch is the only long-lived branch, and listing others invites their
creation. -->

## Short-lived branches

Name: `<pattern>`. Generated by <the host's issue-linked branch creation | the
author, following the pattern>.

Cut from: `<branch>`. Merged into: `<branch>`. Deleted on merge:
<yes, enforced by the repository setting | yes, by convention>.

## Tags and versions

Format: `<pattern>`. Version scheme: <SemVer | CalVer | project scheme>.
Cut from: `<branch(es)>`. Protected: <yes / no>.

## Merge method

| Contribution origin | Method | Why |
|---|---|---|
| Branch in this repository | <rebase / squash / merge commit> | <reason> |
| Fork or outside contributor | <squash / …> | <reason> |

Enforcement: <both methods are enabled repository-wide; the choice is the
merger's, owned by ROLE | only METHOD is enabled>.

## Hotfix path

<Ordered sequence of refs, written as steps an agent can follow. For an
environment chain, state that the fix branch merges into the most downstream
branch first and then into each of the others from the same branch. For release
branches, state upstream first: land on the default branch, then cherry-pick,
naming the original commit in the cherry-pick message.>

## Maintained versions

<Delete unless several versions are maintained.>

| Version | Branch | Supported until | Accepts |
|---|---|---|---|
| `<x.y>` | `<x-y-stable>` | <date or condition> | <what counts as serious enough> |

## Enforcement register

| Rule | Tier | Readback | Upgrade trigger |
|---|---|---|---|
| <rule> | <enforced / advisory / convention> | <command or settings path> | <event that should make it enforced> |

## Update this file when

- A long-lived branch or deployment environment is added or retired.
- The platform's protection or merge-method settings change.
- A deploy job's branch trigger changes; the CI configuration and this file
  name the same branches and must agree.
- A maintained version reaches end of support.
