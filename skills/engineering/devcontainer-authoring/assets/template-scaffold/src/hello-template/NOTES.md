## Usage notes

This is the example template shipped with the repository scaffold.
Replace it with a real starting point (and keep this NOTES.md pattern:
the release workflow appends it to the auto-generated README).

Document here what the generated options table cannot express: what the
template is for, what the user should do right after applying it, and
which files are safe to delete (`optionalPaths`).
